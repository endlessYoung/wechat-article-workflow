---
name: wechat-formatting
description: 将 Markdown 原文或结构化大纲排版为符合微信公众号阅读习惯的最终文本，可直接复制到公众号后台。处理标题层级、段落节奏、代码/引用/列表/分隔线，并在关键位置补引导语与过渡句。
version: 0.1.0
trigger: 用户要求"排版公众号文章 / 格式化公众号文案 / 按公众号规范整理文章 / 输出可直接粘贴到公众号后台的文本"
---

# 公众号文章排版 Skill

## 1. 定位与输入输出

- **输入**：Markdown 原文，或结构化文章大纲（标题 + 要点），或"未排版的长文本"。
- **输出**：符合微信公众号阅读习惯的**最终文本**，可直接复制进公众号后台。
- **边界**：你负责**排版与润色节奏**，不改变原文事实与观点；不新增原文没有的内容（除非用户明确要求补写）；不破坏代码、链接、数据。

## 2. 调用方式（三选一）

1. **手册模式（推荐给 LLM）**：直接按本文档流程执行；规则细节查阅 `rules/`，文案素材查阅 `templates/`。
2. **CLI 模式**：`node bin/wechat-format.js <input.md> -o out.md [选项]`，机械转换交给引擎，你再做创意性加工。
3. **模块模式**：`import { format } from './src/index.js'`，对 `format(source, options)` 的返回值 `{ text, stats, warnings }` 做后续加工。

引擎选项见 `src/options.js` 或 `interface/options.schema.json`（`--json` 可输出结构化结果）。

## 3. 处理流程（六步）

1. **通读原文**：识别主题、读者、语气基调；标记长段、标题层级混乱、缺过渡的位置。
2. **拆结构**：确认 H1/H2/H3 层级关系；H1 做文章主题或大章节，正文层级不超过 3 层；同级标题风格统一。
3. **套规则**：按 `rules/01`~`rules/03` 逐项检查——标点全角、中英文空格、段落切分、代码/引用/列表/分隔线。
4. **补文案**：按 `rules/04` 与 `templates/` 在开头加钩子、章节间加过渡句、结尾加 CTA；**结合上下文改写，不照抄模板**。
5. **校验**：对照下方自检清单；可用引擎跑一遍拿 `warnings` 辅助检查。
6. **输出**：给出最终文本；若用户需要，同时给出"改动说明"（改了哪些标点/段落/层级）。

## 4. 规则速查（详见 rules/）

| # | 规则 | 要点 |
| --- | --- | --- |
| 01 | 标点 | 中文语境用全角标点 `，。？！：；（）`；省略号用 `……`；中英文/数字之间加空格（`每天进步 1%`）；关键句加粗，一屏不超过 2-3 处 |
| 02 | 结构 | 标题最多 3 层；同级标题风格统一（可编号 `01 |`）；段落 1-4 行、单段 ≤ 120-160 字；段间空一行；标题前后空行 |
| 03 | 块 | 代码块保留围栏并标注语言；引用转为「」或后台引用样式并注明出处；列表符号统一、步骤用有序列表；全文分隔线统一（`---` 或 `✦ ✦ ✦`），一篇 1-3 处 |
| 04 | 语气 | 开头 3 行内进入主题；每 2-3 个章节插一次过渡句；结尾给出行动号召或金句；真诚、克制、不喊口号 |

## 5. 模板速查（详见 templates/）

- 开头钩子：`templates/hooks.md`
- 章节过渡句：`templates/transitions.md`
- 结尾与 CTA：`templates/endings.md`
- 列表/引用/代码引导语：`templates/block-intros.md`

使用模板时**务必结合当前段落改写**：过渡句要承接上文、引出下文，禁止生硬照搬。

## 6. 输出自检清单

- [ ] 无超过 160 字的整段文字；段落之间有空行
- [ ] 标题层级正确、同级风格统一；标题前后有空行
- [ ] 中文标点全角；中英文/数字之间有空格的规范（代码与 URL 除外）
- [ ] 代码块围栏完整且有语言标注；引用样式统一且注明出处
- [ ] 列表符号统一；分隔线全文只有一种
- [ ] 开头有钩子、章节间有过渡、结尾有收束/CTA
- [ ] 未破坏原文事实、链接、代码逻辑

## 7. 与 Harness / Plugin 对接

- 工具契约：`interface/tool-contract.md`
- 插件清单示例：`interface/plugin-manifest.example.json`
- 集成设计：`docs/harness-integration.md`
- 示例输入/输出：`examples/input.md`、`examples/expected-output.md`
